discord.py[voice]==2.7.1
yt-dlp>=2026.8.19
python-dotenv==1.2.2
PyNaCl==1.5.0
psutil

# Nur für das eigene Zuhören der Sprachsteuerung (VOICE_OWN_LISTEN=true).
# Ohne diese Pakete läuft der Bot unverändert; er nimmt die Sätze dann über
# POST /command vom DM-Bot entgegen.
discord-ext-voice-recv>=0.5.2a179
faster-whisper>=1.2
numpy>=1.26
